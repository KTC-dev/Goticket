// This file is kept for backward compatibility but is not used in the Supabase version.
// Event data is accessed directly through the Supabase client in the routes.

module.exports = {
  // Legacy Mongoose model - not used
  // All event operations are handled via Supabase client
};
